apiVersion: v1
kind: Service
metadata:
  name: {{ include "person-service-rust.fullname" . }}
  labels:
    {{- include "person-service-rust.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "person-service-rust.selectorLabels" . | nindent 4 }}
