apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "person-service-rust.fullname" . }}-test-connection"
  labels:
    {{- include "person-service-rust.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "person-service-rust.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
